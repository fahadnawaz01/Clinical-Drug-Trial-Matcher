{
  "name": "presigned-url-generator",
  "version": "1.0.0",
  "description": "Lambda function to generate S3 pre-signed URLs for secure file uploads",
  "main": "src/index.js",
  "scripts": {
    "test": "jest",
    "deploy": "./deploy.sh",
    "package": "npm install --production && zip -r function.zip src/ node_modules/"
  },
  "keywords": [
    "aws",
    "lambda",
    "s3",
    "presigned-url",
    "file-upload"
  ],
  "author": "Trial-Scout Team",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/s3-request-presigner": "^3.700.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
